import * as React from 'react';
import * as ReactDom from 'react-dom';
import { Version } from '@microsoft/sp-core-library';
import {
  IPropertyPaneConfiguration,
  PropertyPaneTextField
} from '@microsoft/sp-property-pane';
import { BaseClientSideWebPart } from '@microsoft/sp-webpart-base';

import * as strings from 'TreeViewDemoWebPartStrings';
import TreeViewDemo from './components/TreeViewDemo';
import { ITreeViewDemoProps } from './components/ITreeViewDemoProps';

export interface ITreeViewDemoWebPartProps {
  description: string;
}

export default class TreeViewDemoWebPart extends BaseClientSideWebPart <ITreeViewDemoWebPartProps> {

  public render(): void {
    const element: React.ReactElement<ITreeViewDemoProps> = React.createElement(
      TreeViewDemo,
      {
        description: this.properties.description
      }
    );

    ReactDom.render(element, this.domElement);
  }

  protected onDispose(): void {
    ReactDom.unmountComponentAtNode(this.domElement);
  }

  protected get dataVersion(): Version {
    return Version.parse('1.0');
  }

  protected getPropertyPaneConfiguration(): IPropertyPaneConfiguration {
    return {
      pages: [
        {
          header: {
            description: strings.PropertyPaneDescription
          },
          groups: [
            {
              groupName: strings.BasicGroupName,
              groupFields: [
                PropertyPaneTextField('description', {
                  label: strings.DescriptionFieldLabel
                })
              ]
            }
          ]
        }
      ]
    };
  }
}
